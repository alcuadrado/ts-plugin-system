// Plugins can extend some application objects, but to do that
// in a typescript friendly way, they also need to extend their
// types.
// This file loads those type extensions.

/// <reference types="plugin" />